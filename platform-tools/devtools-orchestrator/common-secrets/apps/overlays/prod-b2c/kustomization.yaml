apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

nameSuffix: -prod-consumer

patches:
- path: argocd-applicationset-patch.yaml
  target:
    kind: ApplicationSet
    name: common-secrets

