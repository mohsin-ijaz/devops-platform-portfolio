apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: schema-registry
  labels:
    app: schema-registry
    release: prometheus-stack
spec:
  endpoints:
  - port: jmx-metrics
    interval: 30s
    path: /metrics
  selector:
    matchLabels:
      app: schema-registry

