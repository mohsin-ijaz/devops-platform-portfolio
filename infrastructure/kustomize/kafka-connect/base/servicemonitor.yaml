apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kafka-connect
  labels:
    app: kafka-connect
    release: prometheus-stack
spec:
  endpoints:
  - port: jmx-metrics
    interval: 30s
    path: /metrics
  selector:
    matchLabels:
      app: kafka-connect
