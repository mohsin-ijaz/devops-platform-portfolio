apiVersion: v1
kind: Service
metadata:
  labels:
    app: kafka-connect
  name: kafka-connect
spec:
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
  selector:
    app: kafka-connect