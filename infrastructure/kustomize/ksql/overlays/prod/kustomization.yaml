apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: false
    pairs:
      env: prod

patches:
- path: ksql-node-patch.yaml
  target:
    kind: Deployment
    name: ksql

resources:
  - ../../base
