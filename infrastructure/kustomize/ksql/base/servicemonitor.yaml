apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ksql
  labels:
    app: ksql
    release: prometheus-stack
spec:
  endpoints:
  - port: jmx-metrics
    interval: 30s
    path: /metrics
  selector:
    matchLabels:
      app: ksql
