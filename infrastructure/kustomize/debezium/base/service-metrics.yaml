apiVersion: v1
kind: Service
metadata:
  name: debezium-metrics
  labels:
    app: debezium
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "5556"
    prometheus.io/path: "/metrics"
spec:
  ports:
    - name: jmx-metrics
      port: 5556
  selector:
    app: debezium
