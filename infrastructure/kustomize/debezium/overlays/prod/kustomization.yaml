apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: false
    pairs:
      env: production

patches:
- path: debezium-patch.yaml
  target:
    kind: Deployment
    name: debezium

resources:
  - ../../base